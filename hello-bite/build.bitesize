project: sample
components:
  - name: spring-app
    os: linux
    repository:
      git: git@github.com:thilinapiy/bitesize-sample-apps.git
      branch: springapp1-v2.0.10
    build:
      - shell: mkdir -p /app
      - shell: mvn clean package
      - shell: cp -rf target/*.jar /app
      - shell: /usr/local/bin/fpm -s dir -n spring-app --iteration $(date "+%Y%m%d%H%M%S") -t deb /app
    artifacts:
      - location: "*.deb"

